/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store

# Local sample/oracle corpus: media blobs are never committed, but the
# fetch script / manifest / checksums under corpus/ are.
/corpus/media/
/fixtures/*.mp4
/fixtures/*.ts
/fixtures/*.m4s
